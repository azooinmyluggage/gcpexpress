apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodejs-deployment
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: nodejs  
    spec:
      containers: 
      - name: nodejs
        image: azooinmyluggage/nodejs:1.0
        ports:
        - containerPort: 3000

